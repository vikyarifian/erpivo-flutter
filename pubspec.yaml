name: erpivo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  # Utility packages
  flutter_native_splash: ^2.4.6
  animated_text_kit: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/logos/erpivo_logo.png
  - assets/images/carousel-1.png
  - assets/images/carousel-2.png
  - assets/images/carousel-3.png
